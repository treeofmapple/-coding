[project]
name = "crud-web"
version = "0.1.0"
description = "Crud Application To Fetch Books"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.2",
    "flask>=3.1.3",
    "pydantic>=2.12.5",
    "uvicorn>=0.42.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
