use binary_tree::structs::{binary_node::BinaryNode, binary_tree::BinaryTree};

fn main() {
    let leaf1 = BinaryNode::new();
    let leaf2 = BinaryNode::new();

    let tree = BinaryTree::new(Some(Box::new(root)));

}
