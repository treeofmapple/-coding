Following flow of ideas
Change the server.rs
To not be a request handler
Let it only handle server functions
On the request handler change it to a diferent file and make it toggle functions
Trigger something instead of blank actions or mocked actions
Implement like a book functionality as an external and transform the server as a lib to serve only as a propourse of a lib and not a full project.
Then implement something like book and try to call it with postman